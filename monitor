FROM python:3.12-slim
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*
WORKDIR /frontend
COPY src-monitor/ .
RUN pip install --no-cache-dir flask requests gunicorn psutil
CMD ["gunicorn", "--bind", "0.0.0.0:6000", "main:app"]