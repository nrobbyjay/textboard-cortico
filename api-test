FROM python:3.12-slim
WORKDIR /app
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*
COPY src-api/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
RUN pip install pytest requests httpx
COPY src-api/ .