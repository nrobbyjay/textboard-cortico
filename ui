FROM python:3.12-slim
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*
WORKDIR /frontend
COPY src-ui/ .
RUN pip install --no-cache-dir flask requests gunicorn
CMD ["gunicorn", "--bind", "0.0.0.0:5500", "main:app"]